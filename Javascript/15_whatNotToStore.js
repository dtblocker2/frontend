/* we should not store sensitive data ie passwords, userdata should not be stored because any script can access your data

nonsensitive data like dark  mode ore lite mode, partially filled formed, cache images or data, amazon cart data, advertisements etc.
 */
